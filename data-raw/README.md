This folder originally contains the raw MODIS, SRTM and ERA5-Land data. It is empty because the data files are too large for GitHub. COntact me or GECO Bern if you wish receive the raw datasets.
The R scripts are scripts for processing the data (to .rds files that are stored in "data").
