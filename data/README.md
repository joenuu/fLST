This folder contains the pre-processed data, as well as all files that are generated during the workflow.

**Warning:** Although this folder looks pretty full, some files that are generated during the workflow were too large to push. You can therefore not run all .Rmd files in "vignettes" without running the scripts in "analysis" because some data might be missing.
